---
export interface Props {
    large?: boolean,
    href?: string
}

const {large, href} = Astro.props;
---

<a
{href}
class:list={[
    "font-bold flex items-center justify-center gap-3 border-2 border-current px-5 py-3",
    large &&  "text-xl",
    ]}
>
<slot name="icon" class="size-8" />
<span><slot/></span>
</a>