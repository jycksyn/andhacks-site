{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    // Alias `src` to `~`
    "baseUrl": ".",
    "paths": {
      "~/*": ["src/*"]
    }
  },
  "mdx": {
    // Enable strict type checking in MDX files.
    "checkMdx": true
  }
}
